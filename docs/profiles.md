# Profiles

V1 ships static presets for:
- `research`
- `creator`
- `operator`

All three use the same kernel. Research is the most complete V1 workflow because live state and connector-backed routines do not land until later phases.

Recommendation:
- keep your own personal notes in a separate vault
- use the agent-owned `wiki/` layer for generated knowledge pages
