# Connector SDK

Not part of V1.

This placeholder exists so the repository shape matches the planned handoff. Connector manifests already have a canonical home at `vault/connectors/`, but connector execution lands in a later phase.
