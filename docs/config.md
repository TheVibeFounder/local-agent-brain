# Config

`brain.config.yaml` is emitted as JSON-compatible YAML.

V1 fields:
- `version`
- `vault_name`
- `profile`
- `mode`
- `created_at`
- `index_path`
- `schema_version`

Supported modes:
- `local-only`
- `hybrid`
- `cloud-assisted`
