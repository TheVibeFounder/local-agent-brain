# Privacy And Security

V1 defaults to `local-only`.

Implications:
- no cloud model capability is required
- no remote connector capability is required
- authoritative state stays in vault files
- `.brain/` can be deleted and rebuilt

Secrets are not written to the vault.
